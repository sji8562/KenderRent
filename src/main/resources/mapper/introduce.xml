<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tenco.toyproject.repository.interfaces.IntroduceRepository">
    <select id="findContentByCode" resultType="com.tenco.toyproject.repository.entity.Information">
        SELECT * FROM information WHERE code = #{code}
    </select>
</mapper>