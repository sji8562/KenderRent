<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--반드시 세미콜론 생략 -->
<mapper
        namespace="com.tenco.toyproject.repository.interfaces.mng.MngSaleRepository">

<!--    <select id="findByRentId">-->
<!--        select * from rent where id = #{id}-->
<!--    </select>-->
    <select id="findSaleAllCount">
        select count(*) from sale
    </select>
    <select id="findSaleWithUserAll">
        select s.id, u.user_name, p.id as productId,p.name as productName, s.status ,s.created_at from sale s left outer join user u on s.user_id = u.id left outer join product p on s.product_id = p.id LIMIT #{start}, 5
    </select>

    <update id="updateBySaleStatus">
        update sale set status = 2 where id = #{id}
    </update>
    <delete id="deleteBySaleStatus">
        delete from sale where id =${id}
    </delete>
</mapper>