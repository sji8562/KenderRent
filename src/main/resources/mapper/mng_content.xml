<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
        namespace="com.tenco.toyproject.repository.interfaces.mng.MngContentRepository">

    <!--<select id="findByRentId" resultType="Map">
        select * from rent where id = #{id}
    </select>-->
    <!--<select id="findSaleAllCount" resultType="int">
        select count(*) from sale
    </select>-->
    <!--<select id="findSaleWithUserAll" resultType="Map">
        select s.id, u.username, p.id as productId,p.name as productName, s.status ,s.created_at from sale s left outer join user u on s.user_id = u.id left outer join product p on s.product_id = p.id LIMIT #{start}, 5
    </select>-->
    <update id="createContent" resultType="int">
        INSERT INTO information (content, type, created_at) VALUES (#{content}, 0, now())
    </update>
</mapper>