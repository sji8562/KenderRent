<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--반드시 세미콜론 생략-->
<mapper namespace="com.tenco.toyproject.repository.interfaces.CustomerRepository">
	<select id="selectCustomer" resultType="map">
		select * from 
			customer c 
			inner join user u on c.user_id = u.id 
			where code = #{code}
			order by c.id desc
			LIMIT #{start}, 5
			
	</select>
	<insert id="insertInquiry">
		insert into customer (user_id, product_id,  code, title, content) 
		values(#{userId}, #{productId},#{code},#{title}, #{content});
	</insert>
	<select id="selectInquiryDetail" resultType="Map">
		select * from customer where id = #{id}
	</select>
	<select id="countCustomer" resultType="Integer">
		select count(*)
		from customer
		where code = #{code}
	</select>
</mapper>